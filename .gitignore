# Track core GAS source files; keep clasp metadata private
.clasp.json
appsscript.json

# Common ignores
*.log
node_modules/
.DS_Store
Thumbs.db

# Windows task scripts: keep PS scripts, ignore secrets/logs/certs
windows-tasks/**/secrets*
windows-tasks/**/*.log
windows-tasks/**/*.tmp
windows-tasks/**/*.bak
windows-tasks/**/*.crt
windows-tasks/**/*.cer
windows-tasks/**/*.pfx
windows-tasks/**/*.key

# Linux tasks secrets
linux-tasks/**/env.sh
linux-tasks/**/secrets*
linux-tasks/**/*.log
